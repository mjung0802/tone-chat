{
  "name": "attachmentsservice",
  "version": "1.0.0",
  "description": "File storage and attachment management service",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "node --env-file=.env --watch --experimental-strip-types src/index.ts",
    "build": "tsc",
    "test": "tsx --test --experimental-test-module-mocks \"src/**/*.test.ts\"",
    "migrate": "node --experimental-strip-types src/db/migrate.ts"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.800.0",
    "express": "^5.2.1",
    "multer": "^2.0.1",
    "postgres": "^3.4.7"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/express-serve-static-core": "^5.1.1",
    "@types/multer": "^1.4.12"
  },
  "license": "ISC"
}
