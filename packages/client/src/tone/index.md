# tone/

- **toneRegistry.ts** — `BASE_TONES` (9 tones: /j, /s, /srs, /lh, /hj, /pos, /neg, /gen, /t), `getBaseTone()`, `customToneToDefinition()`, `resolveTone()` (merges custom with base), `parseToneTag()` (extracts tone from message suffix)
- **toneRegistry.test.ts** — unit tests
