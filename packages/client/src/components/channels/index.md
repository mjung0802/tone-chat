# components/channels/

- **ChannelListItem.tsx** — single channel item with type icon (pound/volume-high), title, topic; `isActive` applies theme-aware tonal highlight; props: channel, isActive, onPress
- **ChannelSidebar.tsx** — 260px left sidebar: server name header, scrollable channel list, create button (if canManage)
