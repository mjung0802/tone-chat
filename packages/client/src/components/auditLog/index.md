# auditLog/

- **AuditLogItem.tsx** — renders a single audit log entry with action-specific icon, "{actor} {verb} {target}" title, metadata description (ban reason, mute duration, role change), and relative timestamp
- **AuditLogItem.test.tsx** — unit tests for all 7 action types, metadata display, fallback to user IDs, display name preference
